package main.java;

import java.util.Map;

import main.resource.model.user;

class app {
    public static void main(String[] args) {
        user[] users;
        Map<Integer, Boolean> rollnums;
        // load the users details from data to listOfUsers
        // fill all the respected roll num using hashmap by iterating over the users
        // give user options
    }
    
}